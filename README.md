docker //servem para gerenciar imagens, contêineres e o ambiente de forma prática na linha de comando.
Sqlliteonline//funciona como uma biblioteca de software leve que grava todos os dados em um único arquivo no disco, sem precisar de um servidor separado
  Primary key sql //é uma restrição que identifica exclusivamente cada registro em uma tabela de banco de dadosIsso garante que a coluna designada (ou combinação de colunas) contenha valores completamente únicos e não possa conter valores NULL 
Banco de dados// Um banco de dados SQL é um sistema que guarda informações em tabelas de linhas e colunas, usando a linguagem SQL para gerenciar e consultar esses dados. exemplos:MySQL, PostgreSQL e SQL Server.
Back-end //é a parte de um sistema ou site que fica nos bastidores e com a qual o usuário não interage diretamente. 
GitHub // é uma plataforma online de hospedagem de código-fonte e arquivos que usa o sistema Git para controle de versão.